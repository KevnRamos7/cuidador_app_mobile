import 'package:flutter/material.dart';

class ProfileClienteMain extends StatelessWidget {
  const ProfileClienteMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}